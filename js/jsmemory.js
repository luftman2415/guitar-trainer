// Lógica específica para la Memoria Musical

function setupMemory() {
    // Aquí iría la lógica para el gestor de canciones
    // Por ahora, solo mensajes en la consola para demostrar que funciona
    document.getElementById('new-song-btn').addEventListener('click', () => alert('Función "Nueva Canción" no implementada.'));
    document.getElementById('import-songs-btn').addEventListener('click', () => alert('Función "Importar" no implementada.'));
    document.getElementById('export-songs-btn').addEventListener('click', () => alert('Función "Exportar" no implementada.'));
}